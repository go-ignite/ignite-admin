## Build Setup

``` bash
# install dependencies
yarn

# serve with hot reload at localhost:8080, and backServer should listen on :8000, if not please change proxy in vue.config.js
yarn serve

# build for production with minification to /static
yarn build

```
